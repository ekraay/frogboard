DATABASE_URL="postgresql://USER:PASS@HOST/DBNAME?sslmode=require"
ORGANIZER_PASSWORD="choose-a-strong-shared-password"
# Secret salt for hashing imported Scout IDs. Required in production; set once and never change it (changing it breaks re-import dedup).
ROSTER_ID_SALT="choose-a-long-random-secret"
